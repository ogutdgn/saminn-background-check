"""Source adapters + the shared contract + the registry.

One file per source. Adapters never import each other; they all return the shapes
defined in `base.py`. See docs/ARCHITECTURE.md and backend/adapters/README.md.
"""
